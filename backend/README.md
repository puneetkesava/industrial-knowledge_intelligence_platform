# Backend bootstrap notes for Industrial Brain AI (Milestone 1.1+)
#
# Install (from backend/):
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   pip install -e ".[dev]"
#
# Run API skeleton:
#   uvicorn app.main:app --reload --app-dir .
#
# Tooling:
#   ruff check .
#   black --check .
#   pytest
