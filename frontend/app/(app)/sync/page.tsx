import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SyncPage() {
  return (
    <PlaceholderPage
      title="Google Drive Sync"
      description="Continuous Intelligent Indexing status and corpus sync controls."
      note="Sync start/status APIs from Milestone 1.6 will bind here later."
    />
  );
}
