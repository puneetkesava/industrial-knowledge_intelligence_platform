import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function MotorsPage() {
  return (
    <PlaceholderPage
      title="Motor Explorer"
      description="Primary navigation — browse, search, and filter motors."
      note="Open a motor detail route at /motors/[id] for the Motor 360 shell placeholder."
    />
  );
}
